const Party = require("../data/models/party");

const createParty = async (request, response) => {
    try {
        Party.create({
            acronymn: 
        })
    } catch (error) {
        
    }
}